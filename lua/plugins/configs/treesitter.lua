local options = {
  ensure_installed = { "lua","python", "javascript", "typescript", "java", "kotlin" },
  auto_install = true,

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
